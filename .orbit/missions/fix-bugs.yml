name: fix-bugs
tags:
  - eval
  - smoke

# Halt thresholds — if the agent fails this many cycles in a row, stop.
# Defaults are 2; explicit here so the v1 smoke is short.
halt_on_consecutive_regressions: 2
halt_on_consecutive_task_failures: 2

# stop_after omitted on purpose — pass --stop-after HH:MM at the CLI
# so the smoke can pick a near-future wall-clock without re-pushing.
